# Supabase — project URL and anon key
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Aurzo SSO (shared across Aurzo apps)
VITE_AURZO_SSO_URL=

# Anthropic (for advisor, composer, gift ideas, date ideas, weekly pulse)
# NOTE: Claude API calls should proxy through the backend — never expose
# this key in the client bundle.
ANTHROPIC_API_KEY=
